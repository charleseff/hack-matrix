# Core RL dependencies
gymnasium>=0.29.0
numpy>=1.24.0

# For training with PPO
stable-baselines3>=2.0.0
sb3-contrib>=2.0.0
torch>=2.0.0

# For visualization and monitoring
tensorboard>=2.14.0
matplotlib>=3.7.0
wandb>=0.15.0
tqdm>=4.65.0
rich>=13.0.0

# JAX for TPU/GPU accelerated training
jax>=0.4.20
jaxlib>=0.4.20
flax>=0.8.0
optax>=0.1.7
chex>=0.1.8
# Note: distrax removed - we use pure JAX for masked categorical (compatible with JAX 0.7.0+)

# Development tools
ruff>=0.1.0

# Testing
pytest>=7.0.0
pytest-xdist>=3.0.0  # parallel test execution

# Pre-commit hooks (using prek - installed separately)